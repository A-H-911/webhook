# Preserve X-Forwarded-Proto set by an upstream proxy (e.g. ngrok passes "https").
# Falls back to $scheme when no upstream proxy header is present (direct local traffic).
map $http_x_forwarded_proto $forwarded_scheme {
    ""      $scheme;
    default $http_x_forwarded_proto;
}
